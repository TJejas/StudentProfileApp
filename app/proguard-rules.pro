# Add project specific ProGuard rules here.
# Minification is disabled for this project (isMinifyEnabled = false), so this
# file is currently unused, but is kept so release builds can enable it later
# without extra setup.
